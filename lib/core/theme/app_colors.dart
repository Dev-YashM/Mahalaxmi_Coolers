import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  static const Color primary = Color(0xFF0C2C55);     // Dark Navy
  static const Color secondary = Color(0xFF296374);   // Teal
  static const Color accent = Color(0xFF629FAD);      // Soft Blue
  static const Color background = Color(0xFFEDEDCE);  // Cream
  static const Color surface = Colors.white;
}